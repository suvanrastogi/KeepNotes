class AppController{
constructor(title,note,time){
    this.title=title; // Initialize the title of the note
    this.note=note; // Initialize the content of the note
    this.time=time; // Initialize the time of the note
}

}

export default AppController;